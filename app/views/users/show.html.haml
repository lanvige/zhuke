.span-15.append-1
  #stream_container
    #main_stream.stream
      = render 'shared/stream', 
        :posts => @posts,
        :user => @user
        
%input{ :type=>'hidden', :id=>'user_slug', :value=>@user.slug}
.span-8.last
  = @user.avatar.thumb.url
  = render "sidebar", :user => @user
