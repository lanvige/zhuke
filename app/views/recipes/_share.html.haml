= simple_form_for( :shared, :url => share_recipe_path(@recipe), :remote => true ) do |f|
  .field
    = f.text_area :content, :style => "width:420px; height:80px;"
    
  .actions
    = f.submit 'Save'